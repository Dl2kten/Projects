#include <iostream>
#include <string>

/*
* Exercise 18.24
* Text presented a series of calls made through a Bear pointer that pointed to
* a Panda object, explain each call assuming using a ZooAnimal pointer pointing
* to a Panda Object instead
*/

int main() {
	/*
	* ZooAnimal *pb = new Panda("ying_yang");
	* pb->print(); // ok: Panda::print(), ZooAnimal defines virtual print,
	* bound at run-time
	* pb->cuddle(); // error: not part of the ZooAnimal interface
	* pb->highlight(); // error: not part of the ZooAnimal interface
	* delete pb; // ok: Panda::~Panda(), ZooAnimal has virtual destructor,
	* Panda inherits
	*/


	return 0;
}