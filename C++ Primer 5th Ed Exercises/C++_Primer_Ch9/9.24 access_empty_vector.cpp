#include<iostream>
#include<vector>
#include<list>
#include<string>
#include <deque>



/*
* Exercise 9.24
* Fetch the first element in a vector using at, subscript, front, and 
* begin, test with empty vector
*/

using std::cout;
using std::cin;
using std::endl;

int main() {

	std::vector<int> ivec;

	//cout << ivec.at(0);
	//cout << ivec[0];
	//cout << ivec.front();
	//cout << *(ivec.begin());

	/*
	* at gave debug error, subscript debug assertion failed, front gave
	* debug assertion failed, begin also debug assertion failed 
	*/
	return 0;
}